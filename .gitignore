log.lammps

legacy/
venv/
.venv/
output/
*.csv
data/
# data/ above only matches a real directory -- data is conventionally a
# symlink named `data` (e.g. under particle-tracking/), which the trailing
# slash doesn't match.
/data
**/__pycache__/
__pycache__/
*.log
*.lammpstrj
**/yolo_dataset/
*.tif
*.tiff

# rf-detr
rf-detr/mlruns/
rf-detr/checkpoints/
rf-detr/checkpoints-a40/
rf-detr/*.pth
rf-detr/*.pt
rf-detr/split/
rf-detr/.env

# data-setup
data-setup/mlruns/
data-setup/lightning_logs/
data-setup/*.db
data-setup/*.db-shm
data-setup/*.db-wal
data-setup/*.bak
data-setup/models/

# yolov12
yolov12/*.pt
yolov12/runs/
yolov12/mlruns/
/runs/

# yolov5 (vendored, not a submodule)
yolov5/

# generated verification output
verification/verification_output/

# lammps-scripts: regeneratable simulation run output (trajectories, logs, plots)
lammps-scripts/results/
lammps-scripts/simulation_results/*.csv
lammps-scripts/simulation_results/*.png

# particle-tracking: regeneratable evaluation output
particle-tracking/evaluation/results/

# generated lint reports
lint-reports/

# Claude Code settings
.claude/
CLAUDE.md

# planning/brainstorm docs (local working notes, not shared repo content)
docs/
.worktrees
